# Remediation Plan — $repo_name

**Assessed:** $generated_at | **Verdict:** $verdict

Ranked blockers below, each mapped to the phase that owns fixing it, with an effort estimate and a
suggested driver (who typically owns this kind of work). Ordered highest-impact first: failing hard gates
before low-scoring dimensions, since gates cap the verdict regardless of anything else.

---

## Blockers

$blockers_table

---

## If Nothing Else Gets Fixed

$minimum_viable_section
